# ─── ClawDex — Production Environment (Vercel) ───────────────────────────────
#
# Copy these to your Vercel project's Environment Variables:
#   → vercel.com/[team]/clawdex/settings/environment-variables
#
# All variables below should be set under the "Production" environment.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# ──────────────────────────────────────────────────────────────────────────────

# ─── Sanity CMS (required) ────────────────────────────────────────────────────
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2026-02-03
# Use a write token with restricted permissions for production
SANITY_API_TOKEN=your_sanity_api_token

# ─── Resend (required for email notifications) ───────────────────────────────
RESEND_API_KEY=your_resend_api_key

# ─── Claude API (required for AI enrichment pipeline) ────────────────────────
ANTHROPIC_API_KEY=your_anthropic_api_key

# ─── Social Monitoring (required for auto-discovery cron jobs) ────────────────
SOCIALDATA_API_KEY=your_socialdata_api_key
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
GITHUB_PAT=your_github_pat

# ─── Site URL (Vercel sets this automatically, override if using custom domain)
# NEXT_PUBLIC_SITE_URL=https://clawdex.com
